services:
  - type: web
    name: kompagnon-algo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: bash start.sh
    envVars:
      - key: KOMPAGNON_API_URL
        value: https://kompagnon.onrender.com

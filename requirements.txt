# Libraries selected for the project
pandas == 3.0.3           # Used for data manipulation and cleaning
scikit-learn == 1.9.0     # Used for machine learning algorithms
numpy == 2.4.6            # Used for numerical operations
pytest == 9.0.3           # Used for testing
SQLAlchemy == 2.0.50      # Used for database operations
psycopg2-binary >= 2.9.12 # Used as Postgres driver for SQLAlchemy
fastapi == 0.136.3        # Used for API to exchange data with the main api
uvicorn == 0.39.0         # Used as ASGI server to run FastAPI
python-dotenv == 1.2.2    # Used to load environment variables from .env
httpx == 0.28.1           # Used for testing API endpoints
pytest-cov == 7.1.0       # Used for coverage reporting
tomli == 2.4.1             # Used for parsing TOML configuration files
